<!DOCTYPE html>
<html>
  <head>
    <title>BodyBoost</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>

  </head>

  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="/dashboard">SportTrack</a>
        <ul class="navbar-nav ms-auto">
          <% if current_user %>
            <li class="nav-item"><%= link_to "Activités", activities_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Repas", meals_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Déconnexion", logout_path, method: :delete, class: "nav-link" %></li>
          <% else %>
            <li class="nav-item"><%= link_to "Connexion", login_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Inscription", signup_path, class: "nav-link" %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type == "alert" ? "danger" : "success" %>"><%= message %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
